<% if Friendship.where(user_id: current_user).count > 0  %>
  <div class="index-center">
    <h3>Bookmark friends</h3>
  </div>

  <% @friend_bookmark.each_slice(4) do |friend| %>
    <div class="row">
      <% friend.each do |key, value| %>
        <div class="icons">

          <% value.each do |i| %>
            <a href="<%= i[1] %>">
              <img class="rounded float-left" height="170" width="200" src="<%= i[2] %>">
            </a><br>
            <%= i[0] %>
            |
          <% end %>
          <%= key %>
        </div>
      <% end %>
    </div>
  <% end %>
<% else %>
  <div class="center-content">
    <h3>You don't have any friends, you can find them
      <a href="/users/index">here</a>,<br>
      or your friends don't have any bookmar
    </h3>
  </div>
<% end %>
